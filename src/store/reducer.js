const defaultState = {
    myPreMsg: [
        {msg: 'Hi, there ~', id: 1},
        {msg: '我，張和潮啦', id: 2},
        {msg: '一個愛前端的社畜', id: 3},
    ]
};

// eslint-disable-next-line
export default (state = defaultState, action) => {
    return state;
}